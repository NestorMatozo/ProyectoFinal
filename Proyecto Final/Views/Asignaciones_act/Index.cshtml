@model IEnumerable<ProyectoFinal_ParteAdmin.Models.Asignaciones_act>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_asignaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.actividad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.detalle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_asignaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.actividad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.detalle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_asignacionesact">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_asignacionesact">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_asignacionesact">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
